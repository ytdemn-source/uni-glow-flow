Add the uploaded Google Search Console verification file to the site root.

The uploaded file `google7eb63b3dfe2b05f5.html` contains:

```
google-site-verification: google7eb63b3dfe2b05f5.html
```

In this Vite app, files placed in `public/` are served at the site root. I will copy the uploaded file to:

```
public/google7eb63b3dfe2b05f5.html
```

After the next publish, it will be reachable at:

```
https://uni-glow-flow.lovable.app/google7eb63b3dfe2b05f5.html
```

This satisfies Google's file-based site verification so the property stays verified. No other changes are needed.